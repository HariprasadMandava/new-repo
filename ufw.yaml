---
 - name: ufw module demo
   hosts: localhost
   become: true
   tasks:
   
    - name: nginx installed
      ansible.buitlin.apt:
          name: "ngnix"
          state: "present"
          update_cache: true

